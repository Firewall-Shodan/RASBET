export { default as authReducer, logout, setUser } from './auth';

export {
  default as cartReducer,
  removeFromCart,
  toggleCart,
  clearCart,
} from './cart';
export { default as oddReducer, addOdd, clear as clearOdds } from './odd';
